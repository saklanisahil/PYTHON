# Assignment 2 


---

## 🧩 Task 1: Check if a Number is Even or Odd

### 📄 Problem Statement
Write a Python program that:
1. Takes an integer input from the user.
2. Checks whether the number is even or odd using an `if-else` statement.
3. Displays the result accordingly.

### 💻 Code Description
- The program takes input from the user using `input()`.
- It uses the modulo operator `%` to check divisibility by 2.
- If the number is divisible by 2, it prints **"Even"**; otherwise, it prints **"Odd"**.

### 🧠 Concepts Used
- Conditional Statements (`if`, `else`)
- Arithmetic Operator (`%`)
- User Input and Output
- terminal opertaor (true experssion if conditon else condition false experssion)

## 🔢 Task 2: Sum of Integers from 1 to N Using a Loop
instead of using 50 numbers i took input from user and then add the number of terms he wanted the sum for 
### 📄 Problem Statement
Write a Python program that:
1. Uses a `for` loop to iterate over numbers from 1 up to `n`.
2. Calculates the sum of all integers in this range.
3. Displays the final sum.

### 💻 Code Description
- Initializes `total = 0`
- Iterates using `for i in range(1, n + 1)`
- Adds each number to `total`
- Prints the total sum

### 🧠 Concepts Used
- Loops (`for`)
- Arithmetic Addition (`+=`)
- Range Function


